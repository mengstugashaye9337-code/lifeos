name: lifeos
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  riverpod_annotation: ^4.0.2
  go_router: ^17.2.3
  # Database
  drift: ^2.16.0
  sqlite3_flutter_libs: ^0.5.20
  path_provider: ^2.1.2
  path: ^1.9.0
  flutter_markdown: ^0.6.0
  http: ^1.2.0
  supabase_flutter: ^2.0.0
  freezed_annotation: ^3.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  # Add these for Riverpod code generation
  riverpod_generator: ^4.0.0
  drift_dev: ^2.16.0
  build_runner: ^2.4.8
  freezed: ^3.0.0
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true

  